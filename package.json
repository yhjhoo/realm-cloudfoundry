{
    "name": "my-ros-app",
    "version": "1.0.0",
    "description": "My new ROS App",
    "main": "src/index.js",
    "scripts": {
        "build": "rm -rf dist; ./node_modules/.bin/tsc",
        "clean": "rm -rf dist",
        "start": "npm run build && node dist/index.js",
        "start-only": "npm run build && node dist/index.js"
    },
    "dependencies": {
        "realm-object-server": "2.5.1",
        "typescript": "2.5.3"
    }
}
